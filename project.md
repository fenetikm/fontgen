# Project

## Overview
Fontgen (to be renamed later).
CLI that let's you output a font (TTF or OTF) where each glyph is dots or lines or patterns or other symbols that are generated via a simple description.
This is useful when working with TUIs.

## Architecture
Golang.
CLI, will use bubbletea framework.
